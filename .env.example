# Copy to `.env` in this directory (never commit `.env`).
# Docker Compose reads this file for variable substitution on `db` and `server`.
#
# Generate a strong password (hex is URL-safe for DATABASE_URL):
#   openssl rand -hex 24
#
POSTGRES_PASSWORD=
