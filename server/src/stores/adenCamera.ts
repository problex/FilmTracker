import { createShopifyAdapter } from "./shopify.js";

export const adenCameraAdapter = createShopifyAdapter({
  storeId: "aden-camera",
  storeName: "Aden Camera",
  baseUrl: "https://www.adencamera.com",
});

